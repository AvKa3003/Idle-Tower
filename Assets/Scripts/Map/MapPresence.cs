namespace IdleTower.Map
{
    /// <summary>Глобальный слой клетки: туман / только вид / интеракция. Не в сейве.</summary>
    public enum MapPresence
    {
        Fog = 0,
        VisibleOnly = 1,
        Interactive = 2
    }
}
